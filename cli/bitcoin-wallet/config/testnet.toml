# configuration for testnet

network = "testnet"
sats_per_byte = 5

# RPC connection: set WALLET_RPC_URL, WALLET_RPC_USER, and WALLET_RPC_PASSWORD env vars
# (or pass --rpc-url, --rpc-user, --rpc-password CLI flags)

# Root path for wallet databases (UTXOs and pending transactions)
db_path = ".union_bridge/bitcoin-wallet"

