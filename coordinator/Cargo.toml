[package]
name = "coordinator"
version = "0.1.0"
edition = "2024"

[dependencies]
check-fork = { path = "../check-fork" }
common = { path = "../common" }
actors-mocking = { path = "../actors-mocking" }
check-fork-zkp = { path = "../check-fork/zkp" }

alloy-primitives = { workspace = true }
alloy-sol-types = { workspace = true }
anyhow = { workspace = true }
bincode = { workspace = true }
clap = { workspace = true }
log = { workspace = true }
serde_json = { workspace = true }
union-contracts = { workspace = true }
primitive-types = { workspace = true }
# TODO this should be a temporary dep, until BitVMX Broker types are extracted to a separate crate
bitvmx-client = { workspace = true }
hex = { workspace = true }
mockall = { workspace = true }
serde = { workspace = true }

reqwest = { version = "0.12", features = ["json", "blocking"] }
uuid = { version = "1.11.0", features = ["v4"] }

[features]
anvil = []
