refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} [{l}] {m}\n"

root:
  level: debug
  appenders:
    - stdout

loggers:
  tarpc: # from bitvmx-broker
    level: warn