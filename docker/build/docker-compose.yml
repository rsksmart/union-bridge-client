#####################################################################################################################################
# DISCLAIMER: This docker-compose file is intended for development and testing purposes only, not for production use. ###############
#####################################################################################################################################

services:
  block-indexer:
    platform: ${PLATFORM}
    image: ghcr.io/rsksmart/union-client-block-indexer:${UC_TAG:-latest}
    build:
      context: ../../
      dockerfile: docker/build/Dockerfile
      args:
        - CRATE=block-indexer
      ssh:
        - default=${SSH_AUTH_SOCK}
    volumes:
      - ${CONFIG_DIR:-../../config}:/app/config/:ro
      - ${LOGGER_PATH:-./log4rs.yaml}:/app/log4rs.yaml:ro
      - db-blocks:/app/db/blocks
      - ${BLOCK_INDEXER_BROKER_PEM_PATH}:/app/broker.pem:ro
    environment:
      - ENV_NAME=${ENV_NAME:-dummy-env-name}
    restart: no
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
      test: ["CMD", "pgrep", "-f", "block-indexer"] # TODO improve with health endpoint
    entrypoint: ["sh", "/app/key-setup.sh"]
    command: ["sh", "-c", "/app/block-indexer --env $ENV_NAME"]

  log-indexer:
    platform: ${PLATFORM}
    image: ghcr.io/rsksmart/union-client-log-indexer:${UC_TAG:-latest}
    build:
      context: ../../
      dockerfile: docker/build/Dockerfile
      args:
        - CRATE=log-indexer
      ssh:
        - default=${SSH_AUTH_SOCK}
    volumes:
      - ${CONFIG_DIR:-../../config}:/app/config/:ro
      - ${LOGGER_PATH:-./log4rs.yaml}:/app/log4rs.yaml:ro
      - db-logs:/app/db/logs
      - ${LOG_INDEXER_BROKER_PEM_PATH}:/app/broker.pem:ro
    environment:
      - ENV_NAME=${ENV_NAME:-dummy-env-name}
    restart: no
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
      test: ["CMD", "pgrep", "-f", "log-indexer"] # TODO improve with health endpoint
    entrypoint: ["sh", "/app/key-setup.sh"]
    command: ["sh", "-c", "/app/log-indexer --env $ENV_NAME"]

  coordinator:
    platform: ${PLATFORM}
    image: ghcr.io/rsksmart/union-client-coordinator:${UC_TAG:-latest}
    build:
      context: ../../
      dockerfile: docker/build/Dockerfile
      args:
        - CRATE=coordinator
      ssh:
        - default=${SSH_AUTH_SOCK}
    volumes:
      - ${CONFIG_DIR:-../../config}:/app/config/:ro
      - ${LOGGER_PATH:-./log4rs.yaml}:/app/log4rs.yaml:ro
      - ${COORDINATOR_BROKER_PEM_PATH}:/app/broker.pem:ro
      - keystore:/keystore
      - ../../resources:/app/resources:ro
      - db-coordinator:/app/db/coordinator
    restart: no
    environment:
      - UB__coordinator__bitvmx__host=${BITVMX_HOST:-dummy-bitvmx-host}
      - UB__coordinator__bitvmx__port=${BITVMX_PORT:-dummy-bitvmx-port}
      - UB__coordinator__advance_funds__check_fork_guest_elf_path=${UB__coordinator__advance_funds__check_fork_guest_elf_path:-} # TODO change this before productive release
      - UB__BRIDGE__COMMITTEE__DRP_PROGRAM_DEFINITION=${UB__BRIDGE__COMMITTEE__DRP_PROGRAM_DEFINITION:-}
      - UB__COORDINATOR__BLOCKS__PUBKEY_HASH=${UB__COORDINATOR__BLOCKS__PUBKEY_HASH:-}
      - UB__COORDINATOR__LOGS__PUBKEY_HASH=${UB__COORDINATOR__LOGS__PUBKEY_HASH:-}
      - UB__COORDINATOR__USER__PUBKEY_HASH=${UB__COORDINATOR__USER__PUBKEY_HASH:-}
      - KEY_STORE_PASSWORD=${KEY_STORE_PASSWORD}
      - BITCOIN_NETWORK=${BITCOIN_NETWORK:-dummy-bitcoin-network}
      - ENV_NAME=${ENV_NAME:-dummy-env-name}
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
      test: ["CMD", "pgrep", "-f", "coordinator"]
    entrypoint: ["sh", "/app/key-setup.sh"]
    command: ["sh", "-c", "/app/coordinator --env $ENV_NAME"]
    depends_on:
      block-indexer:
        condition: service_healthy
      log-indexer:
        condition: service_healthy
      user-api:
        condition: service_healthy

  user-api:
    platform: ${PLATFORM}
    image: ghcr.io/rsksmart/union-client-user-api:${UC_TAG:-latest}
    build:
      context: ../../
      dockerfile: docker/build/Dockerfile
      args:
        - CRATE=user-api
      ssh:
        - default=${SSH_AUTH_SOCK}
    volumes:
      - ${CONFIG_DIR:-../../config}:/app/config/:ro
      - ${LOGGER_PATH:-./log4rs.yaml}:/app/log4rs.yaml:ro
      - ${USER_API_BROKER_PEM_PATH}:/app/broker.pem:ro
      - keystore:/keystore
    environment:
      - UB__USER_API__COORDINATOR__PUBKEY_HASH=${UB__USER_API__COORDINATOR__PUBKEY_HASH:-}
      - KEY_STORE_PASSWORD=${KEY_STORE_PASSWORD}
      - ENV_NAME=${ENV_NAME:-dummy-env-name}
      - USER_BITCOIN_WIF=${USER_BITCOIN_WIF:-}
    restart: no
    ports:
      - "${USER_API_PORT:-40001}:40001"
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
      test: ["CMD", "pgrep", "-f", "user-api"]
    entrypoint: ["sh", "/app/key-setup.sh"]
    command: ["sh", "-c", "/app/user-api --env $ENV_NAME"]

volumes:
  keystore:
  db-blocks:
  db-logs:
  db-coordinator:
