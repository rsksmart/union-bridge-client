services:
  bitvmx-client:
    # Published by the upstream BitVMX client project; override if you mirror the image or need a different ref.
    image: ghcr.io/fairgatelabs/rust-bitvmx-workspace/bitvmx-client:v0.7.1
    # container_name: bitvmx-client-container
    restart: unless-stopped
    entrypoint: [ "./run.sh" ]
    environment:
      RUST_BACKTRACE: full
      CLIENT_OP: ${CLIENT_OP}
    healthcheck:
      test: [ "CMD-SHELL", "pgrep -f bitvmx-client" ] # Check bitvmx-client process is running
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - db-bitvmx:/tmp

volumes:
  db-bitvmx:
