[package]
name = "check_fork"
version = "0.1.0"
edition = "2021"

[dependencies]
primitive-types = { workspace = true, features = ["std", "serde"] }
serde = { workspace = true, features = ["derive"] }
