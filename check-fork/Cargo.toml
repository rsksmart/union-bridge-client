[package]
name = "check-fork"
version = "0.1.0"
edition = "2024"

[lints.clippy]
all = "deny"

[dependencies]
primitive-types = { workspace = true }
serde = { workspace = true, features = ["derive"] }
