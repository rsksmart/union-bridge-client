[package]
name = "check-fork"
version = "0.1.0"
edition = "2024"

[dependencies]
primitive-types = { workspace = true, features = ["std", "serde"] }
serde = { workspace = true, features = ["derive"] }
