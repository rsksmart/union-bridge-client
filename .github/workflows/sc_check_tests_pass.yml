name: All Tests

permissions:
  contents: read
  pull-requests: write
  statuses: write

on:
  pull_request:
    types: [ opened, synchronize, reopened, ready_for_review ]
    branches:
      - main
  merge_group:
    types: [ checks_requested ]
    branches:
      - main

jobs:
  crates-tests:
    name: 'Crates Tests'
    uses: ./.github/workflows/wf_crates_tests.yml
    secrets: inherit

  qa-tests:
    name: 'QA Tests'
    uses: ./.github/workflows/wf_qa_tests.yml
    secrets: inherit